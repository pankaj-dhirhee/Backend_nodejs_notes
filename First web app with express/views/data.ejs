<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Receiving data from an array </title>
</head>
<body>
  <h2> Printing data inside an array as it is </h2>
  <p> <%= array_of_employees %> </p>
  
  <br>
  
  <h2> Printing an object as it is </h2>
  <p> <%= array_of_employees[0] %> </p>
  
  <br>
  
  <h2> Printing a particular data indide an object of employees array </h2>
  <p> Name: <%= array_of_employees[0].name %> </p>
  <p> Roll no: <%= array_of_employees[0].roll_no %> </p>
  
  <br>
  
  <h2> Printing data using loops </h2>
  <h4> 1. Using forEach loop: </h4>
  <% array_of_employees.forEach((obj, index) => { %>
    <p> <%= Object.keys(obj)[index] %>: <%= obj.name %> </p>
    <p> <%= Object.keys(obj)[index] %>: <%= obj.roll_no %> </p>
  <% }); %>
  
  <h4> 2. Using while loop</h4>
  <%let i = 0; %>
  <% while(i < array_of_employees.length){ %>
    <p> Name: <%= array_of_employees[i].name %> </p>
    <p> Roll no: <%= array_of_employees[i].roll_no %> </p>
    <% i++ %>
  <% }; %>
</body>
</html>